{
  "name": "impala67-sync-server",
  "version": "2.2.3",
  "description": "Impala67 E2EE Real-Time Sync Worker on Cloudflare",
  "main": "worker.js",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dry-run": "wrangler deploy --dry-run",
    "db:init": "wrangler d1 execute impala67-db --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.124.0"
  }
}
