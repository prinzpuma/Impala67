window.APP_CONFIG = {
  // OAuth-Client vom Typ "Webanwendung". Die Client-ID ist öffentlich und kann
  // alternativ einmalig in der PWA unter Einstellungen → Sync hinterlegt werden.
  // Niemals API- oder Client-Secrets in eine statische PWA eintragen.
  GOOGLE_WEB_CLIENT_ID: "DEINE_WEB_CLIENT_ID.apps.googleusercontent.com"
};
